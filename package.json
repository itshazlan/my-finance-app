{
  "name": "my-finance-app",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "db:push": "pnpm --filter @repo/api exec prisma db push",
    "db:migrate": "pnpm --filter @repo/api exec prisma migrate dev",
    "db:studio": "pnpm --filter @repo/api exec prisma studio",
    "db:generate": "pnpm --filter @repo/api exec prisma generate",
    "db:reset": "pnpm --filter @repo/api exec prisma migrate reset"
  },
  "devDependencies": {
    "prettier": "^3.7.4",
    "turbo": "^2.8.15",
    "typescript": "5.9.2"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  }
}
